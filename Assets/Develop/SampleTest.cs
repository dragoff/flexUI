using System.Collections;
using System.Collections.Generic;
using FlexUI;
using UnityEngine;

public partial class SampleTest : BaseView
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
